//Bài 2 - ID: BT002 - Đường đi trong ma trận
#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t; cin >> t;
    while(t--){
        int m, n, x1, y1, x2, y2;
        cin >> m >> n;
        int arr[m+1][n+1];
        for(int i=0; i< m; i++)
            for(int j=0; j<n; j++)
                cin >> arr[i][j];
        cin >> x1 >> y1 >> x2 >> y2;
    }
    return 0;
}